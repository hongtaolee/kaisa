<div class="min-nav">
	<div class="img"></div>当前位置：
	<a href="/" title="">首页</a>&gt;&gt;
	<a href="/articles/<%=@article.id %>" title=""><%=@article.title %></a>
</div>

<div id="content" class="show-content footer-content">
	<div class="aside">
    <% @page.blocks.each do |block|%>
    <%=raw block.content %>
    <% end if !@page.blank? %>
	</div>
	<div class="main">
		<h1><%=@article.title %></h1>
		<h6 class="text-center"><%=@article.title %></h6>
    <div><%=raw @article.content %></div>
	</div>
</div>
